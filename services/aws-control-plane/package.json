{
  "name": "@axl/aws-control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS process-host assembly for the Axl control plane",
  "license": "Apache-2.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json --force",
    "start:deployment-test": "node dist/deployment-test-runtime.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1134.0",
    "@axl/control-plane": "workspace:*",
    "@axl/protocol": "workspace:*",
    "jose": "6.2.12"
  }
}
